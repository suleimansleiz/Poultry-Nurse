<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    xmlns:app="http://schemas.android.com/apk/res-auto">

   <androidx.cardview.widget.CardView
       android:layout_width="match_parent"
       android:layout_height="wrap_content"
       android:padding="5dp"
       app:cardCornerRadius="10dp"
       android:layout_margin="5dp"
       app:cardElevation="10dp"
       app:cardBackgroundColor="@color/white_bluish">


        <RelativeLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_marginTop="5dp"
            android:layout_marginLeft="5dp"
            android:layout_marginRight="5dp"
            android:padding="5dp">

            <TextView
                android:id="@+id/tvAIName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:drawablePadding="10dp"
                android:textColor="@color/purple_700"
                android:text="@string/ai_notification"
                android:fontFamily="@font/gotham_bold"
                app:drawableStartCompat="@drawable/circle_unread_icon"
                app:drawableTint="@color/red" />

            <TextView
                android:id="@+id/tvRecyclerNote"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_gravity="center_vertical"
                android:layout_below="@+id/tvAIName"
                android:layout_marginTop="5dp"
                android:text="@string/morning_brief"
                android:fontFamily="@font/gotham_book"
                android:textColor="@color/black"/>

            <TextView
                android:id="@+id/tvArrowForward"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_alignParentEnd="true"
                android:layout_centerInParent="true"
                android:textAppearance="@style/TextAppearance.Material3.TitleMedium"
                app:drawableEndCompat="@drawable/arrow_forward_icon"
                tools:ignore="RelativeOverlap"
                app:drawableTint="@color/purple_700" />
        </RelativeLayout>

   </androidx.cardview.widget.CardView>

</LinearLayout>